# PR History

Per-PR change log. Newest at bottom. The CLAUDE.md table format is preserved here verbatim.

## PR status

| PR   | Branch         | Status | Contents                                                                    |
| ---- | -------------- | ------ | --------------------------------------------------------------------------- |
| 1    | merged to main | ✅      | Scaffold, AppLogger, RotatingFileSink, AppSettings, ConfigStore             |
| 2    | merged to main | ✅      | RpApiClient, ApiModels, CookieProvider, StubURLProtocol, fixtures           |
| 3    | merged to main | ✅      | KeychainStore, KeychainCookieProvider, LoginWindowController                |
| 4    | merged to main | ✅      | AudioDeviceCatalog                                                          |
| 5a   | merged to main | ✅      | libmpv vendoring + RPSmoke CLI                                              |
| 5b   | merged to main | ✅      | PlayerEngine (libmpv Swift actor)                                           |
| 6    | merged to main | ✅      | PlaybackCoordinator                                                         |
| 7    | merged to main | ✅      | AppKit shell (NSStatusItem + borderless NSPanel hosting placeholder)        |
| 8    | merged to main | ✅      | MiniPlayerView (SwiftUI) + AppDelegate real-graph wiring                    |
| 9    | merged to main | ✅      | NotificationCoordinator + AlbumArtCache + album art in MiniPlayerView       |
| 10   | merged to main | ✅      | SettingsView + rating row + KeychainCookieProvider + login flow             |
| 11   | merged to main | ✅      | AppContainer composition root + App/Edit main menu                          |
| 12   | merged to main | ✅      | Smoke fixes + UI polish (rp.ico, Layout E, live bitrate, cue, hog mode)     |
| 12.5 | merged to main | ✅      | Event-cursor block resume (drops now_playing API; per-channel cursor)       |
| 13   | merged to main | ✅      | api/play migration (replaces get_block; supports favorites chan=99)         |
| 14   | merged to main | ✅      | Telemetry endpoints (update_history, update_pause) for cross-session resume |
| 15   | merged to main | ✅      | GitHub Actions: swift test on push; universal .app bundle on tag push       |
| 16   | merged to main | ✅      | Popover + Settings UI polish (support link, song-in-browser, hamburger menu, ZStack channel row, title3 song title) |
| 17   | merged to main | ✅      | Audio device error handling (errors stream, popover auto-open, VM reset)    |
| 18   | merged to main | ✅      | Ambient background from album art (opt-in; fades on promo/error/disable)    |
| 19   | merged to main | ✅      | Upcoming Program window (get_block read-only fetch, multi-column card view, settings row count + channel filter; ambient card color; cue Int/String fix; black progress bar in light+ambient mode) |
| 20   | merged to main | ✅      | CI: merge release into ci.yml (test + tag-gated release jobs); Codecov coverage via OIDC + sersoft lcov conversion + codecov.yml path fix; README badges (build+tests, coverage, latest release, license) |
| 21   | merged to main | ✅      | Audio settings overhaul: releaseHogOnPauseEnabled, forceMaxVolumeEnabled (replaces dead softwareVolumeEnabled), applyReplayGainEnabled, DeviceVolumeController (CoreAudio scalar pin/read), PlaybackState stream + currentPlaybackState, force-max confirmation alert, ReplayGain hover info popover, Appearance segmented picker, output-device refresh button (AudioDeviceCatalog.reload() on protocol) |
| 22   | merged to main | ✅      | Media-key + Now Playing center support: MPRemoteCommandCenter (play/pause/togglePlayPause/nextTrack), MPNowPlayingInfoCenter (title/artist/album/artwork/duration/elapsed + playbackState); position throttled to 1 Hz; artwork construction in nonisolated helper to avoid MainActor-isolation crash on MediaPlayer's dispatch queue |
| 23   | merged to main | ✅      | Popover shared components: PopoverAlbumArt + SongTitleRow + AmbientGradientBackground extracted; PastSongView gets ambient gradient + matched typography; PastSongPopoverController folded into shared PopoverController via present(rootView:relativeTo:); main popover panel width 320 → 342; rating moved to title row (.title3); year displayed inline with album (right-aligned) |
| 24   | claude/pr24-stale-block-recovery | ✅      | Stale-block detection + long-idle resume refetch: BlockSongs.isStale (cue=0, all elapsed<=0 with at least one strictly negative — distinguishes stale music block from fresh promo); play(channelId:) advances via action=play with last song's event/type/sliceNum (single retry, accepts second stale response without recursing); resume() refetches via play() when paused >=59 min (CDN TCP idle eviction defense); resume() block-expiration check now goes through injected clock() for testability |
| 25   | merged to main | ✅      | Popover style picker (None / Ambient / Frosty) replaces Ambient + Liquid Glass toggles after user testing showed Apple's `.glassEffect()` produced unpredictable results across light/dark modes; AppSettings.popoverStyle: PopoverStyle (default .none) — setPopoverStyle keeps ambientBackgroundEnabled in sync; PopoverController takes configStore, builds container NSView with NSVisualEffectView (.hudWindow, .behindWindow, hidden by default) sibling to NSHostingView, toggles isHidden on popoverStyle == .frosty; MiniPlayerView/PastSongView switch on viewModel.popoverStyle for `.background` (Ambient wraps gradient in ZStack with windowBackgroundColor base for opacity parity); progress-bar high-contrast styling extended to all non-.none styles in light mode; UpcomingWindowController rebuilt around same container pattern + sets window.isOpaque=false when frosted so .behindWindow blur reaches desktop |
| 26   | merged to main | ✅      | Per-device audio settings persistence: `AudioProfile` struct + `AppSettings.audioProfiles: [String: AudioProfile]` keyed by output-device UID; `AppContainer.live()` settings binder is profile-aware — on `outputDeviceUID` change, restores stored profile (hog / force-max / replaygain / release-on-pause) atomically into top-level fields + pushes to `HogModeController` / `DeviceVolumeController` / `MpvPlayerEngine`; first-time devices seeded with safe defaults (hog OFF, force-max OFF, replaygain ON, release-on-pause ON); `SettingsViewModel` setters write back to active device's profile entry; `SettingsView` Audio section split into Output Device picker + per-device "Output device settings" labeled section with device name in header. Eliminates the cross-device force-max-on-built-in-speakers leak (the dangerous failure mode after incoming-call / sleep / device-unplug forced hog OFF) |
| 27   | merged to main | ✅      | CPU + RAM mpv tweaks (pre-scan quick wins; deep scan deferred — see Deferred section): `time-pos` observer `MPV_FORMAT_DOUBLE` → `MPV_FORMAT_INT64` (1 Hz native vs 10–25 Hz; bridge casts `Int64` → `Double` so the `PlayerEvent.positionUpdate(seconds: Double)` enum is unchanged); `mpv_wait_event` pump timeout 0.5s → 5.0s (idle wakeups dropped 10×; shutdown still uses `mpv_wakeup`); demuxer caps `demuxer-max-bytes=8MiB` + `demuxer-max-back-bytes=1MiB` + `cache-secs=10` (RAM ceiling ~300 MB → ~80 MB on live stream — mpv defaults sized for video scrubbing, wasted on a stream we never rewind); removed redundant `MainActor.run` in `MiniPlayerViewModel` position handler; `NowPlayingCenterController`'s 1 Hz throttle becomes redundant defense-in-depth (kept) |
| 28   | merged to main | ✅      | Gapless block transitions via mpv playlist append-play: PlayerEngine surface adds queueNext / advanceToQueued / clearPlaylist + PlayerEvent.fileStarted (MPV_EVENT_START_FILE) + baseline option prefetch-playlist=yes; Coordinator widens prefetch trigger to last-song-start (drops <10s remaining guard, hooks emitNowPlaying so single-song promo/favorites/1-song-music blocks queue immediately); absorbPrefetchResult queues the next block on mpv's playlist with a post-await currentBlock!=nil race guard; queuedToEngine flag gates dual-path EOF handler (case .fileStarted → swapToPrefetchedBlockState when queued; case .fileEnded(.eof) → fallback engine.play(replace) only when not queued); skipForward past-last fast-paths to engine.advanceToQueued; changeChannel/stop/handlePlaybackError/advancePastUnplayableBlock/resume long-idle all clearPlaylist + reset queuedToEngine; swapToPrefetchedBlockIfAvailable's engine.play throw now emits to errorsContinuation (was silent log) |
| 29   | merged to main | ✅      | Update Checker: `UpdateChecker` actor polls GitHub releases (`api.github.com/repos/gvajda/rp-player/releases/latest`) on startup + every 24h, filters out prerelease/draft, parses `tag_name` via `SemVer.parse` (strips `v`/`V` prefix + `-prerelease` suffix), publishes `AsyncStream<UpdateState>` (multi-subscriber, per-call continuation, snapshot-seeded). `AppSettings.updateCheckEnabled` (default true) + `lastUpdateCheckAt` + `dismissedUpdateVersion` + `cachedLatestRelease` (full `ReleaseInfo` Codable). `start()` seeds state from cache before checkNow; `tickIfDue()` skips when toggle off or < 24h elapsed; dismissed-tag auto-resets when latest > dismissed. `NoopUpdateChecker` injected when unbundled (no `CFBundleShortVersionString`). UI: Settings → Updates section (toggle, Check Now, last-checked + version status); MiniPlayer popover replaces "RP Player" text with bordered "Update Available" button (`arrow.up.forward.square` SF Symbol) when available + not dismissed-from-button; ContextMenuBuilder adds sticky "Update Available…" item between About and Quit when `updateAvailableForMenu != nil`. `UpdatePanelController` hosts NSPanel with `UpdatePanelView`: title, relative published date, ~5-line markdown body preview, [Later] / [View Full Notes] (opens `htmlUrl`) / [Download DMG] (opens first `*.dmg` `browser_download_url` — hidden when no DMG asset). `AppDelegate` late-binds `viewModel.openUpdatePanel`. |
| 30   | claude/pr30-long-idle-stall-guard | ✅ | Long-idle resume stall guard: mpv baseline `network-timeout=15` (default 60s; stuck `ffurl_read` now fails fast → existing `handlePlaybackError` recovery); `LivePlaybackCoordinator` stall watchdog armed in `resume()`'s long-idle/expired branch — `armLongIdleStallWatchdog` captures `currentPositionSeconds` + `startsAt[currentSongIndex]>0` retry start synchronously on the actor, then spawns `[weak self]` Task that races `positionUpdates` (filtered `pos != snapshot` to skip seeded value) against injected `sleep(timeoutNs)` via `withTaskGroup`; on first 10s timeout: `engine.stop` + `engine.play(url:startSeconds:)` retry once; on second 10s timeout: `surfaceStallError` mirrors `handlePlaybackError` cleanup (clearPlaylist, cancel prefetch, nil all state, emitState(.stopped)) + yields `"Playback stalled. Try Pause/Play to recover."` — no `onDeviceUnavailable` (network stall ≠ device disappear); `cancelStallWatchdog` wired into 8 cleanup sites (`play`/`pause`/`stop`/`changeChannel`/`shutdown`/`handlePlaybackError`/`swapToPrefetchedBlockState`/`swapToPrefetchedBlockIfAvailable`); `sleep` injected at coordinator init alongside `clock` for deterministic tests via `ControllableSleep` helper (NSLock + `withCheckedContinuation`, `releaseAll` resumes parked + sets `manualReleaseAll` so retry's second sleep also returns immediately); 6 new tests cover: positionUpdate clears watchdog (no retry), single timeout triggers retry (engine.stop + 3rd engine.play), double timeout surfaces error + clears state, stop/pause cancel watchdog (no retry after release), fresh-block resume <59min doesn't arm |
| 31   | claude/pr31-gapless-migration | ✅ | api/gapless migration: replaces `api/play` (and `api/get_block` for Upcoming) with the new endpoint that returns a flat song list, each song a self-contained file URL with own `cue` / `duration` / `event_id` / `type` (M/P) / `slice_num`. New types: `GaplessSong` + `GaplessResponse` (Decodable, custom `init(from:)` handling `song_id` Int-or-String + empty `album` → nil). New API method: `RpApiClient.gapless(channel:bitrate:numSongs:)` (sorted query, cookies attached for chan=99 favorites support). `LivePlaybackCoordinator` rebuilt around `queue: [GaplessSong]` + `currentResponse: GaplessResponse?` + `refetchTask: Task<Void, Never>?` (replaces `currentBlock` + `orderedSongs[]` + `startsAt[]` + `currentSongIndex` + `prefetchedBlock` + `prefetchTask` + `queuedToEngine`). Boundary advance via `MPV_EVENT_START_FILE`: telemetry for queue[0] (just-finished) → `queue.removeFirst()` → emit NowPlaying for new head → `engine.queueNext(queue[1])` → `kickRefetch()` if `queue.count < 3`. `kickRefetch` race-guards on both `currentChannelId` AND `queue.first?.eventId` snapshots; queue-merge filters `eventId > queue[0].eventId` then `queue = [queue[0]] + newSongs`. `skipForward`: `queue.count >= 2` → `engine.advanceToQueued()`; else sync refetch + restart. `resume()` long-idle (≥ 59 min) drops queue + refetches via gapless; PR 30 stall watchdog still armed. `handleSongPlaybackError` (replaces `advancePastUnplayableBlock`): drop queue[0] on mpv -16, advance; counter caps at 3 consecutive failures. Stale-block detection (PR 24) removed — gapless cursor + per-song self-contained URLs make recovery redundant. `NowPlaying` shape: `song: GaplessSong`, `songDurationSeconds`, `bitrateLabel` (drops `songIndexInBlock` / `blockDurationSeconds` / `songStartSeconds` / `songEndSeconds` / `blockBitrate`). Upcoming: one `api.gapless(numSongs: max(rowCount*2, rowCount+5))` call per channel (was: multi-block stitching). Drops: `RpApiClient.play`, `RpApiClient.getBlock`, `PlayAction`, `GetBlock`, `BlockSongs`. `PlayListSong` stays (notification-click `info → PlayListSong` path via `SongRegistry`); `PlayListSong.init(from: GaplessSong)` extension added. `slice_num: Int` in gapless converted to `String(...)` at telemetry call sites. `MockRpApiClient` extended with `gaplessByChannel` + `gaplessResponses` + delay knob. Test factory `makeGaplessSong` / `makeGaplessResponse` hoisted to `Tests/RPPlayerTests/Helpers/GaplessFactories.swift`. |
| 32   | merged to main | ✅      | SongFileCache + download-then-play: new actor `LiveSongFileCache` (SHA256(gaplessUrl)-keyed file store under `~/Library/Application Support/RP Player/SongFileCache/`, in-flight dedup, nonisolated `cachedFile(for:)` + `expectedLocalPath(for:)`, LRU cap `maxFiles=10`); `LivePlaybackCoordinator` pre-downloads each gapless song to disk and hands mpv a local `file://` URL via `engine.play` / `engine.queueNext`, eliminating the boundary HTTP-fetch latency that `prefetch-playlist=yes` cannot remove (per mpv#6437 — prefetch opens the URL but does not pull body bytes). `kickSequentialDownload()` runs at most one outstanding download, walking `queue.dropFirst().prefix(2)` (2 ahead) sequentially; restarted on every queue mutation. `syncQueueHeadFromMpv` matches mpv's `path` property against BOTH `song.gaplessUrl` AND `cache.expectedLocalPath(for: song).path` (mpv now reports the local file path; without dual-matching the head never advanced after the first song). Eviction is event-driven (after dropped-song telemetry spawn) + LRU on every successful write. `stop` / `changeChannel` / `shutdown` / `handlePlaybackError` / `applyBitrateChange` cancel `downloaderTask` + call `cache.cancelInFlightDownloads()` (cancels each in-flight `Task<URL?, Never>`; URLSession.data honors Task cancellation so network bandwidth releases immediately) — they do NOT call `cache.clear()`, so files persist across channel changes for return-to-channel reuse, with LRU as the sole deletion mechanism. Falls back to remote URL when the cache returns nil. Album-art prefetch bumped to queue[1]+queue[2] to match song-file depth. Adds: `SongFileCache` protocol, `LiveSongFileCache` actor, `NoopSongFileCache` (private in AppContainer), `ConfigPaths.songFileCacheDirectory`, `MockSongFileCache` test helper. |
| 33   | claude/pr33-long-idle-resume-rework | ✅ | Long-idle resume rework: `LivePlaybackCoordinator.resume()` no longer cancels the paused, locally-cached song after a multi-hour pause. New flow: probe `songFileCache.cachedFile(for: queue[0])` (cache-miss → legacy `clearPlaylist + queue=[] + currentResponse=nil + lastStartedEventId=nil + currentPositionSeconds=0 + current=nil + pausedAt=nil + pausePositionMs=0 + play(channelId:)` fallback); `engine.resume()` (mpv unpauses queue[0], queue[1] still queueNext'd in mpv playlist); fire `updateHistory(pauseFlag: true)` telemetry via `Task.detached`; reset `pausedAt = nil` + `pausePositionMs = 0`; if `pausedFor ≥ 59 min` truncate `queue` to `prefix(2)` + cancel `downloaderTask` + fire-and-forget `Task { await cache.cancelInFlightDownloads() }` + `kickRefetch()` for background catch-up. `kickRefetch` filter generalized: snapshots both `queue.first?.eventId` (headEvent) AND `queue.last?.eventId` (tailEvent), race-checks all three (channel + head + tail), filters response by `eventId > tailEvent`, merges as `self.queue + filtered` (preserves entire prefix; appends new tail). Boundary advance from queue[1]→queue[2] is the silent catch-up to backend-current. Drops PR 30 stall watchdog (`armLongIdleStallWatchdog`, `cancelStallWatchdog`, `waitForFirstPositionUpdate`, `logStallWatchdogTimeout`, `surfaceStallError`, `stallWatchdog` field, `stallWatchdogTimeoutSeconds` constant, `sleep:` init param + `private let sleep` field, 7 `cancelStallWatchdog()` call sites, `LivePlaybackCoordinatorStallWatchdogTests.swift` file with inline `ControllableSleep` helper + 6 tests, dead `markQueueHeadPending` helper) — sole arm site was the long-idle resume branch; with cached file:// playback the network-stall failure mode is unreachable. Adds `MockSongFileCache.cachedFileOverride` for eviction simulation, `internal snapshotQueueIds()` test seam (via `@testable import`), `Tests/RPPlayerTests/Helpers/WaitUntil.swift` async polling helper. New tests: `testKickRefetchFiltersByQueueLastEventIdAndAppendsTail`, `testLongIdleResumePreservesCachedSongAndQueueOne`, `testLongIdleResumeMergesFreshTailAfterRefetch`, `testLongIdleResumeWithCacheMissForQueueZeroFallsBack`, `testLongIdleResumeKicksRefetchAfterShortQueue`, `testSecondResumeDuringInFlightRefetchIsIdempotent`. |
| 34   | claude/pr34-volume-mode-picker | ⏳ | Volume picker rework: AppSettings + AudioProfile drop `forceMaxVolumeEnabled` + `applyReplayGainEnabled` bool pair in favor of single `volumeMode: VolumeMode` enum (`none` / `replayGain` / `forceMax`); JSON decode migrates legacy bools (force-max wins). SettingsView replaces two toggles with a 3-state Button row matching the existing Appearance / Menu bar icon / Popover style row patterns (Button + `StableButtonStyle`); Force Max button uses `.disabled(!hogModeEnabled)` (SwiftUI honors this on Button, unlike `Picker(.segmented)`'s per-tag disable which is a no-op for segmented style); destructive-confirmation alert fires from the Force Max Button's action handler. Single ⓘ `HoverInfoIcon` to the left of the buttons with combined ReplayGain + Force Max tooltip text. Hog OFF→ON ground-truth check is demote-only: downgrades `volumeMode .forceMax → .none` when device volume is below max, does NOT silently promote `.none → .forceMax` when device IS at max (the bidirectional sync was a property of the old bool toggle and is intentionally removed). AppContainer binder reads enum directly — the dual-bool effective-RG calculation collapses to a single equality check. Safety-wipe sites clear entire `volumeMode` instead of only `forceMaxVolumeEnabled`. "(bit-perfect)" suffix removed from Hog toggle label; the claim moves into the Force Max section of the tooltip. |
| 35   | claude/pr35-parametric-eq-mvp | ⏳ | Parametric EQ MVP: `PlayerEngine.setAudioFilterChain(_:)` → `mpv_set_property_string("af", ...)`; `AudioProfile.eqEnabled` + `eqPresetName` (filename reference, not inline preset) + Codable migration via `decodeIfPresent` defaults; `EqPresetStore` actor (`LiveEqPresetStore` at `~/Library/Application Support/RP Player/EqPresets/<name>.txt`, sanitized filenames — reject `/`, NUL, leading `.`, empty, ≥256 chars; save/loadText/delete/list/exists; case-insensitive sort); `EqModels` (`EqBandType` / `EqBand` / `EqPreset` — Codable + Equatable + Sendable); strict `EqPresetParser` (any warning → `.failure(.warningsNotPermitted(reasons:))` — unsupported types LP/HP/NO/AP/BP, >10 bands, malformed lines all reject; OFF filters silently skipped; empty file → `.failure(.empty)`); `EqPresetWriter` (round-trip from parsed form; `Filter N` numbers compact 1..N over enabled bands); `EqChainBuilder` (Preamp → `volume=<n>dB`; PK → `equalizer=f=:t=q:w=:g=`; LS/HS → `lowshelf`/`highshelf`; returns nil when zero enabled bands AND preamp==0); AppContainer EQ binder Task (`internal static func runEqBinder` + `applyEqState`) watches `(eqEnabled, eqPresetName)` per active device and calls `engine.setAudioFilterChain(...)`; cache-miss / parse-failure / missing-file → setAudioFilterChain(nil); existing audio binder's profile write-back patched to preserve EQ fields across non-EQ settings changes; `SettingsViewModel` adds `eqEnabled` / `eqPresetName` / `availablePresets` published, `presetsRefreshTask` cancelled in `stop()`, setters `setEqEnabled` / `setEqPresetName`, `importPresetFile(url:overwrite:) -> ImportOutcome` (.imported / .nameCollision; rejects non-.txt extension; rejects parser warnings; .nameCollision returned only when save throws `.alreadyExists`), `exportPreset(to:)` copies stored .txt verbatim, `prepareDeletePreset(name:)` → referencing device UIDs, `deletePresetConfirmed(name:)` nil-outs refs + removes file + refreshes; SettingsView "Equalizer" block inside `deviceSettingsSection` after `volumeRow` (toggle, picker with `(None)` + trash icon, Import/Export buttons, squig.link caption, collision-overwrite + delete-confirm alerts); RPSmoke `--probe-filters` flag retained as diagnostic (vendored libmpv 0.36 confirms equalizer / lowshelf / highshelf / volume all OK). |
| 36   | claude/pr36-crossfeed | ⏳ | Crossfeed for headphone listening: per-device `crossfeedEnabled` (default false) + `crossfeedStrength` (default 0.2) + `crossfeedRange` (default 0.5) on `AudioProfile` (Codable `decodeIfPresent ?? <default>` migration, no `AppSettings` top-level mirror — same shape as EQ); new pure-function `CrossfeedFilterBuilder.buildPart(strength:range:)` produces `crossfeed=strength=...:range=...` lavfi fragment with internal clamp (NaN → 0, then 0...1) + `format` helper byte-for-byte identical to `EqChainBuilder.format`; `EqChainBuilder.build(_:) -> String?` split into `buildParts(_:) -> [String]` (no `lavfi=[...]` wrapper) so the binder can concatenate EQ + crossfeed fragments; `AppContainer.runEqBinder` + `applyEqState` renamed to `runAudioFilterBinder` + `applyAudioFilterState` with internal `AudioFilterKey: Equatable` 5-tuple (eqEnabled / eqPresetName / crossfeedEnabled / crossfeedStrength / crossfeedRange) snapshot diff — single `mpv af` write per change; volume/hog binder write-back gains 3 `existing.crossfeed*` passthroughs so non-crossfeed settings changes don't wipe state; chain order locked Preamp → EQ → Crossfeed (preamp stays at head to preserve AutoEQ headroom semantics); `SettingsViewModel` adds `crossfeedEnabled` / `crossfeedStrength` / `crossfeedRange` published (`public private(set)`), setters clamp Doubles via `min(1.0, max(0.0, value))` before write; new `ClampedNumericField` SwiftUI view (TextField + native Stepper) with `ClampedNumericFieldLogic` enum (locale-aware `parse(_:locale:)` — `NumberFormatter` first, `Double(trimmed)` fallback; `isValid(_:in:)` checks `!isNaN && range.contains`; `format(_:)` uses `%.2f`) — red-glow `RoundedRectangle.stroke(.red.opacity(0.85), lineWidth: 1.5)` on invalid input, snap-back to last valid value on `@FocusState` blur; `SettingsView.crossfeedSection` mounted in `deviceSettingsSection` after `eqSection` (same `HStack(spacing: 8)` inline-row pattern), single `HoverInfoIcon` tooltip covers Strength + Range semantics + Bauer-style/BS2B + headphones-only caveat, both numeric fields `isEnabled: viewModel.crossfeedEnabled` so they grey out when toggle is off; `RPSmoke --probe-filters` extended with `crossfeed: OK` assertion (vendored libmpv `audio-encodersgpl` confirms). |
| 37   | claude/device-reattach-on-hog | ⏳ | Device reattach when hog: `AppContainer.handleDeviceLost` (preserve when `hogModeEnabled` / hearing-safety reset when off) shared by `onDeviceUnavailable` closure AND the catalog-changes watcher; `AppContainer.spawnReattachWatcher` one-shot Task subscribed to `CoreAudioDeviceCatalog.changes` re-acquires hog + (if Force Max) `volumeController.setVolumeMax` when held UID reappears, then calls `onReattached` via MainActor.run. New `DeviceReattachState` @MainActor holder (`heldUID` / `reattachTask` / `lastKnownDeviceNames` / late-bound `onReattached`) glues the disconnect sites + reattach callback together; `CoordinatorBox: @unchecked Sendable` late-bind pattern resolves the forward-capture (closure is built BEFORE `LivePlaybackCoordinator` exists). `PlaybackCoordinator.emitUserMessage(_:)` seam lets the handler push the friendly "DeviceName disconnected — waiting for it to come back." banner via the existing errors stream; coordinator no longer hardcodes the -14 message (falls through to generic "Playback stopped unexpectedly (error N)" when no handler set). `MiniPlayerViewModel.clearErrorMessage()` dismisses the banner on reattach (errors stream can't yield nil). User-override detection in the existing `store.changes` Task cancels the watcher + clears the banner when the user picks a different device OR toggles hog off while held; `store.changes` Task moved to AFTER MiniPlayerViewModel construction to bring `viewModel` into scope. `SettingsViewModel` adds `@Published deviceNameCache: [String: String]` populated from each catalog snapshot + computed `disconnectedDevice: AudioDevice?` synthesizing a "<name> (disconnected)" / "Unknown device (disconnected)" row when the saved UID isn't in the live device list; `currentDeviceName` falls back to the cache so the device-settings section title survives a disconnect. `SettingsView` picker injects the synth row between the placeholder and the live `ForEach`. Startup-clear path at AppContainer.swift:106-129 unchanged (no parity decision — relaunch still wipes). New `DeviceReattachTests` (5 tests, exercises handleDeviceLost preserve/reset branches + spawnReattachWatcher reappear + cancellation) + `SettingsViewModelDisconnectedRowTests` (4 tests, covers nil/present/synth/fallback label). |

| 38   | claude/bs2b-crossfeed-upgrade | ⏳ | BS2B Crossfeed Upgrade: replaces FFmpeg `crossfeed` filter with FFmpeg `bs2b` (Bauer stereo-to-binaural). `CrossfeedProfile` enum (`cmoy`/`jmeier`/`custom` — `bs2bDefault` removed after UI review; Custom seeds with bs2b's named-default values 700 Hz / 4.5 dB on entry from any other profile); `AudioProfile` schema swap (`crossfeedStrength`+`crossfeedRange` → `crossfeedProfile`/`crossfeedFcut: Int`/`crossfeedFeedDb: Double`) with legacy-key fallback to Chu Moy defaults on decode (PR 36 profiles seed `.cmoy / 700 / 6.0`, no value mapping since the algorithms are different) and legacy `crossfeedProfile=="default"` strings remap to `.custom` + seed 700 / 4.5 on decode; `CrossfeedFilterBuilder.buildPart(profile:fcut:feedDb:)` emits `bs2b=profile=<name>` for named profiles or `bs2b=fcut=<f>:feed=<int>` for custom (`feed` is in 10×dB units, e.g. 45 = 4.5 dB); public static `CrossfeedFilterBuilder.fcutRange` (300...2000) + `feedDbRange` (1.0...15.0) shared with VM clamps + UI ranges; UI is a 3-button profile picker (Chu Moy / Jan Meier / Custom) using existing `StableButtonStyle(filled:)` + `ForEach(CrossfeedProfile.allCases)`, with a conditional `crossfeedCustomFields` row (fcut + feed `ClampedNumericField`s with Int↔Double bridge on fcut, fcut uses `decimals: 0`) shown only when `crossfeedProfile == .custom`; custom row right-aligned via full-width `HStack` + leading `Spacer(minLength: 0)` + trailing `Toggle("").labelsHidden().opacity(0).allowsHitTesting(false)` anchor that pins the trailing column to the same width as the visible Toggle in the row above; chain order locked Preamp → EQ → bs2b; tooltip describes bs2b semantics succinctly (acoustic leakage simulation, headphones-only) and notes Custom's seed values, drops Qudelix-5K and FFmpeg-internals references; new Bitrate + Hog mode `HoverInfoIcon` tooltips in `bitrateRow` / `hogModeRow` helpers (Bitrate: "Applied on the next song"; Hog mode: exclusive device ownership, 44.1 kHz sample-rate lock, other apps lose audio until pause/quit); `RPSmoke --probe-filters` adds `bs2b: OK` assertion alongside `crossfeed: OK`; vendored libmpv rebuilt with `--enable-libbs2b` from `gvajda/libmpv-darwin-build` fork — adds `Vendor/libmpv/lib/libbs2b.dylib` (~50 KB, MIT); API version `MPV_MAKE_VERSION(2, 1)` = 131073 unchanged. Vendor rpath fix (commit `ac2fef0`): rewrote all 16 dylibs' `@rpath/lib<sibling>.dylib` references to `@loader_path/…` + ad-hoc re-signed; without this the poisoned `LC_RPATH` entries in libmpv.dylib / libfftools-ffi.dylib (pointing at a stale audio-default nix-store build dir) caused dyld to silently load the wrong libavfilter at runtime, manifesting as cold-start "No such filter" errors. See `Vendor/libmpv/README.md` for the rewrite procedure (must be redone on every re-vendor until the fork emits clean install names upstream). `ClampedNumericField` widened from 56×18 to 72×22 with `.font(.system(size: 12))` + `decimals: Int = 2` parameter — wider 4-digit fcut values (`1650`) and 5-character feed values (`12.00`) no longer trigger the TextField vertical-baseline shift originally fixed in commit `f526544` (that fix's 56×18 frame was tuned for narrower content). |

| 39   | claude/eq-preset-editor | ⏳ | EQ Preset Editor (2026-05-21): per-preset edit panel inside Settings → Equalizer with grid bands (one row per band: Type / Frequency / Gain / Q + trash icon, plus Add band up to 10); filter-type dropdown covers Bypass / Peak / Low Shelf / High Shelf (Bypass = `enabled=false`, round-trips as `OFF` rows in the on-disk `.txt`); Save / Save As / Rename actions (`EqPresetStore.rename(from:to:)` updates every `AudioProfile.eqPresetName` reference atomically with rollback on store failure); live audio preview via `EqEditingOverride` actor side-channel (`Sources/RPPlayer/Config/EqEditingOverride.swift`) — the audio filter binder reads the in-memory override (when set) instead of the on-disk preset, so keystrokes reach the lavfi chain in real time without flipping the chain for unrelated devices that reference the same preset name; preset name cap dropped from 255 to 30 characters (UI label length is the binding constraint, longer import filenames truncate on first save); parser preserves `OFF` rows as `enabled: false` (previously silently skipped); writer emits `OFF` rows with sequential `Filter N` indexing over all bands (enabled + disabled); `applyAudioFilterState` signature widened to consult the override actor; old `AudioFilterKey` struct removed (binder diffs directly against an override-aware key tuple); read-only "show parsed values" eye icon dropped in favor of the always-editable panel. Save shows a confirmation alert when the preset is referenced by 2+ devices (`presetReferenceCount`) so the cascade is opt-in; 1-device saves skip the alert. 511 → 540 (+29). |

| 40   | claude/pr40-eof-recovery-nonblocking | ⏳ | EOF recovery non-blocking (2026-05-22): fixes a silent-playback cascade where a short promo's `.fileEnded(.eof)` recovery awaited `songFileCache.localFile(for: next)` on the actor — when the next song's download wasn't done yet, the actor block queued the promo's own EOF behind the wait and the queue/mpv state desynced (UI showed "playing", audio was silent). Fix: recovery now uses synchronous `cachedFile(for:)` (never blocks); on cache miss it defers `queueNext` and emits `.loading`. New `tryQueueNextIfPending(landed:)` helper fires from `kickSequentialDownload` after each download lands — logs `elapsedSinceDeferMs`, lifts state from `.loading` → `.playing`. Cascade-protection lift at end of recovery branch handles the case where the queue collapses to <2 before the queueNext branch could re-defer. New diagnostic logs: recovery-entry cache state, downloader-landed marker. Plan §C's fifth diagnostic log (`recovery: skipping queueNext — queue mutated mid-await`) was intentionally not implemented because Task 2's refactor eliminated the mid-await window (no more `await localFile` inside the queueNext path). Deferred: 5 other `await songFileCache.localFile(...)` sites in `PlaybackCoordinator` have the same blocking-actor risk — see Deferred section below. 540 → 543 (+3). |

| 41   | claude/pr41-tryqueuenextordefer-helper | ⏳ | tryQueueNextOrDefer helper (2026-05-22): generalises PR 40's sync-probe-or-defer pattern. Extracts a private helper on PlaybackCoordinator that does a synchronous cachedFile(for:) probe; on hit calls engine.queueNext, on miss sets deferredQueueNextAt + emits .loading. Converts 5 call sites: play(), skipForward shallow-refetch, applyBitrateChange, handleSongPlaybackError, syncQueueHeadFromMpv. The skipForward mid-skip site (queue[1] not yet queued in mpv) is intentionally NOT converted — mpv is idle waiting for our advanceToQueued, no events can queue behind the await, and converting would force the user's skip to bail out on cache miss. Head-resolve sites (L175, L429, L624, L754) also excluded — they precede engine.play(url:) which needs the URL; converting would mean handing mpv the remote URL on miss (behaviour change, not just risk fix). 4 per-site defer tests + 1 cross-cutting lift test (5 new; the skipForward shallow-refetch defer test was dropped because of an unavoidable race between kickRefetch and skipForward — both call api.gapless concurrently and runRefetch typically pre-fills the queue, forcing skipForward through mid-skip instead). Side-effect: MockSongFileCache.markDownloaded signature changed from Set<Int> to [GaplessSong] so releasedMirror stores the song's actual gaplessUrl (instead of a synthetic mock URL); cachedFile-driven queueNext assertions in tests now match the URL the engine actually receives. 543 → 548 (+5). |
| 42   | claude/pr42-transport-buttons-cache-aware | ⏳ | Cache-aware transport buttons + stop action (2026-05-23): popover transport slot 2 (right of play/pause) now renders three modes depending on `(isPlaying, nextReady, isPaused)`. Playing + next track preloaded → skip button (unchanged). Playing + next not yet queued in mpv → loading spinner (disabled). Paused → stop button (`stop.fill`) that calls coordinator `stop()` and clears the popover's now-playing / album art / palette / rating / bitrate / elapsed-duration state, returning the UI to fresh-launch look while preserving channel selection. New `PlaybackCoordinator.nextReady` signal (`queue.count >= 2 && queueNextEventId == queue[1].eventId`) + `nextReadyUpdates: AsyncStream<Bool>` broadcast — updated at every `queue` / `queueNextEventId` mutation in `LivePlaybackCoordinator` (~21 sites) via new `updateNextReady()` helper. `MiniPlayerViewModel` republishes the signal as `@Published nextReady`. Mid-playback `.loading` emits removed from `skipForward()` queue[1]-not-queued branch, `tryQueueNextOrDefer` defer, EOF-recovery defer, and `tryQueueNextIfPending` lift — `.loading` is now reserved for session-startup-only (play initial + skipForward shallow-refetch). Fixes the "song plays but loading icon is still on" symptom — slot 1's pause icon stays available throughout active playback. View model's `.stopped` switch arm split from `.paused, .stopped`; `.stopped` clears nowPlaying/currentArt/lastLoadedCoverPath/ambientTopColor/currentRating/currentBitrateLabel/songElapsed/songDuration/lastNotifiedSongId. 5 position-related tests in `MiniPlayerViewModelTests` updated to `fireState(.playing)` before `start()` (stateUpdates replay-on-subscribe now reaches the new `.stopped` clear). 6 coordinator tests rewritten to assert `nextReady` instead of the removed `.loading` flicker. New tests: `nextReady` initial / play / defer / stop / stream-replay (5), `skipForward` no-`.loading` regression (1), VM mirror + stopPlayback clearing (2), play-after-stop state machine (1). 548 → 559 (+11). Known UX issue (not fixed): `skipForward()` shallow-refetch path still emits `.loading` while mpv is still playing the old track — brief flicker before audio actually interrupts; pre-existing minor cosmetic, deferred. |
| 43   | claude/pr43-eq-picker-switch-while-editing | ⏳ | EQ preset picker switch while editing (2026-05-23): the Settings EQ preset picker is now safe to change while the edit panel is open. Clean editor → reseeds to the new preset's values silently. Bypass clean → closes the editor. Dirty editor → "Unsaved EQ changes" alert with 4 buttons: Keep editing (picker snaps back, edits intact), Discard changes (drops edits + switches + reseeds), Save changes (writes to original + switches + reseeds; disabled in new-preset mode), Save as new preset… (opens a name sheet, writes to new name, then switches picker to the user's ORIGINALLY-PICKED target — NOT the save-as name). New `SettingsViewModel` API: `requestPresetSwitch(to:)` picker entry point, `cancelPendingSwitch()`, `resolvePendingSwitchDiscard()`, `resolvePendingSwitchSave()`, `resolvePendingSwitchSaveAs(name:)`, plus published `pendingPresetSwitch: PendingPresetSwitch?`. Internally `saveEditAs` extracted a private `writePresetFile(name:)` helper so save-as resolution can write without changing the picker; private `performSwitch(to:)` does the atomic picker change + editor reseed. SwiftUI alert + name sheet attached to `eqSection`, gated by a `savingAsInProgress` `@State` flag to prevent the auto-dismiss-after-button-tap from clearing pending state before the save-as sheet OK handler runs. 559 → 572 (+13). Subtle quirk during impl: `setEqPresetName` mutates configStore but the published `eqPresetName` only refreshes via the async stream subscriber — `performSwitch` adds one extra `await MainActor.run { self.eqPresetName = target }` hop before `reloadParsedPreset()` to avoid reparsing the stale name. |

## Deferred / tech debt

Items surfaced by past reviews / ad-hoc investigation. Non-blocking; pick up if a related area is touched.

### PR 27 — CPU / RAM footprint deep scan (optional)

Pre-scan quick wins landed in `b046cc2`: `time-pos` observer `MPV_FORMAT_DOUBLE`→`INT64` (1 Hz native), `mpv_wait_event` pump 0.5s→5.0s, demuxer caps `demuxer-max-bytes=8MiB` + `demuxer-max-back-bytes=1MiB` + `cache-secs=10` (RAM ceiling 300MB→80MB), removed redundant `MainActor.run` in `MiniPlayerViewModel` position handler. Current footprint ~1.5–7% CPU / ~80 MB RAM is fine; original "15–25% CPU" was likely an Activity Monitor process-attribution misread.

If a deep scan happens later: Instruments Allocations / Leaks. Open suspects if RAM still grows: NSImage retention (popover state, `MPMediaItemArtwork` closures), SwiftUI hosting-view leaks across popover open/close, `URLSession.shared` response data, NSHostingView / NSVisualEffectView retention in `PopoverController` container view. Past-song popover's bounded ≤1 VM leak (see Notifications section in architecture.md) — close-callback follow-up still tracked. SwiftUI re-render audit (ambient gradient + frosted view) only worth doing if a hot path is found.
