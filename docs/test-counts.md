# Test counts by PR

Running tally of `swift test` count after each PR / significant change. Latest at bottom.

- After PR 1: 13
- After PR 2: 18
- After PR 3: 35
- After PR 4: 47
- After PR 5a: 48
- After PR 5b: 67
- After PR 6: 93
- After PR 7: 101
- After PR 8: 111
- After PR 9: 127
- After PR 10: 172
- After PR 11: 184
- After PR 12: 213
- After PR 12 follow-ups (pull-based bitrate, cue-seeded song index): 214
- After libmpv hog vestige cleanup (engine.setHogMode + hogModeChanged event + coordinator hog fallback removed; `LibmpvPlayerEngine` renamed to `MpvPlayerEngine`): 206
- After bitrate-display fix (NowPlaying.blockBitrate + `BlockBitrateLabel` raw-string display; popover shows `block.bitrate` from API instead of mpv's runtime audio-bitrate observer): 210
- After stream-format pipeline removal (deleted `StreamFormat`, `PlayerEvent.streamFormatChanged`, mpv `audio-bitrate` property observer + handler, coordinator `currentStreamFormat`, view-model `currentStreamFormat`; pipeline became dead once display switched to `block.bitrate`): 199
- After now_playing-based song match + elapsed-based offsets (added `RpApiClient.nowPlaying`, `NowPlayingEntry`, `resolveStart` in coordinator; `BlockSongs.startsAtSeconds` reads `elapsed` instead of summing durations; replaced cue-seeded tests with nowPlaying-match + cue-fallback tests; settings bitrate picker shows the 7-option API mapping): 201
- After event-cursor block resume (channelCursors map, drop now_playing API path, deterministic next-block fetch via event=endEvent, prefetch-adoption + cancellation in skipForward past-last): 208
- After promo block fix (PlayListSong.album optional; promo block decode test + fixture): 209
- After popover visual polish (positionUpdates stream + RatingMenu + edge-to-edge art + Quit menu + press-opacity buttons; deletes RatingRow): 217
- After popover polish round 2 (Appearance setting + outline play button + ★/☆ rating label + centered picker w/ bitrate@ + inline RP Player; drops verbose-logging caption + footer): 222
- After notification click → past-song popover (SongRegistry + identifier suffix + NotificationClickRouter + PastSongView + PastSongPopoverController + PlayListSong(from: SongInfo); review fixes: bidirectional mutual exclusion + restored cancellation comment + registry-record test): 246
- After PR 13 api/play migration (drops getBlock + channelCursors; per-install rp3\_ player_id; drops GetBlock.filename; supports favorites chan=99): 251
- After PR 14 telemetry (update_history + update_pause; 5 trigger sites; clock injection; promo/favorites guards): 265
- After PR 16 UI polish (SettingsView support link, MiniPlayerViewModel URL-open methods, channelRow ZStack + hamburger menu, title3 song title, secondary album color, small picker): 265
- After PR 17 audio device error handling (errors stream on coordinator, handlePlaybackError, VM showPopoverIfNeeded injection, StatusItemController.showPopoverIfNeeded; 7 new tests): 272
- After PR 18 ambient background from album art (`AmbientPaletteExtractor` actor, `AppSettings.ambientBackgroundEnabled`, `MiniPlayerViewModel` configStore + paletteExtractor wiring, gradient `.background` + 0.4s ease-in-out animation in `MiniPlayerView`, re-extract on toggle ON mid-playback; 15 new tests): 287
- After PR 19 upcoming program window (`getBlock` restored, `UpcomingProgramViewModel` + `UpcomingColumn` + `UpcomingSongRow` models, `UpcomingProgramView` card/column/skeleton, `UpcomingWindowController`, `AppSettings.upcomingRowCount` + `upcomingHiddenChannelIds`, Settings "Upcoming Program" section, hamburger menu item, `AlbumArtCache.defaultMaxFiles` bumped to 100; ambient card color, promo filter, cue Int/String fix, black progress bar in light+ambient; 22 new tests): 309
- After PR 21 audio settings overhaul (`releaseHogOnPauseEnabled` + `forceMaxVolumeEnabled` replacing dead `softwareVolumeEnabled` + `applyReplayGainEnabled`; `DeviceVolumeController`; `PlaybackState` enum + `stateUpdates` stream + `currentPlaybackState`; engine `setForceMaxVolume`/`setApplyReplayGain`; UI: indented "Release on Pause" + "Force Max Volume", segmented Appearance picker, output-device refresh button, ReplayGain hover info icon, force-max confirmation alert; 2 new state-stream tests): 324
- After PR 22 media-key + Now Playing center support (`NowPlayingCenterController`, `MPRemoteCommandCenter` handlers, `MPNowPlayingInfoCenter` info dict, position throttled to 1 Hz, artwork built in `nonisolated static func` to avoid MainActor-isolation crash on MediaPlayer's dispatch queue): 324
- After PR 23 popover shared components (`PopoverAlbumArt` + `SongTitleRow` + `AmbientGradientBackground` extracted to `Sources/RPPlayer/Shell/Components/`; `PastSongViewModel` gains `configStore` + `paletteExtractor` + `ambientTopColor` published prop + `stop()` cancellation method; `PastSongView` renders ambient gradient with `.easeInOut(0.4s)` animation; `PastSongPopoverController` removed in favor of `PopoverController.present(rootView:relativeTo:)` — `AppDelegate` holds the past-song VM ref and stops it on transitions; main popover panel width corrected 320 → 342 to match album-art width): 334
- After PR 24 stale-block + long-idle resume recovery (`BlockSongs.isStale(songs:cue:)` requiring `cue==0 && all elapsed<=0 && at least one strictly negative` to distinguish stale music block from fresh promo; `play(channelId:)` post-bootstrap stale check + 1 advance retry via `action=play` with last song's event/type/sliceNum; `resume()` refetches via `play()` when `pausedAt` >= 59 min; `Date()` → injected `clock()` migration in `resume()`): 345
- After PR 25 Popover style picker (None / Ambient / Frosty) — replaces Ambient + Liquid Glass toggles after user testing showed `.glassEffect()` visually inverted across light/dark; `AppSettings.popoverStyle: PopoverStyle` (Codable, default `.none`); `SettingsViewModel.setPopoverStyle(_:)` keeps `ambientBackgroundEnabled` in sync; `PopoverController` takes `configStore`, builds container NSView with hidden NSVisualEffectView (`.hudWindow`, `.behindWindow`) sibling to NSHostingView, subscribes to `configStore.changes` and toggles `frostedView.isHidden` on style change; popover SwiftUI views switch on `viewModel.popoverStyle` for `.background` (Ambient wraps gradient in `ZStack { Color(nsColor: .windowBackgroundColor); AmbientGradientBackground(...) }` so the 0.4-opacity bottom stop sits over an opaque base); high-contrast progress-bar styling (`AmbientProgressStyle(fillColor: .black)`) gated on `colorScheme == .light && popoverStyle != .none` so Frosty also gets it; `UpcomingWindowController` rebuilt around the same container pattern (was previously addSubview-of-NSHostingView which left blur invisible) and flips `window.isOpaque = false` + clears `backgroundColor` when frosted is on; `LiquidGlassBackground` SwiftUI ViewModifier removed: 355
- After PR 28 Gapless block transitions — engine surface (`PlayerEngine.queueNext`/`advanceToQueued`/`clearPlaylist` + `PlayerEvent.fileStarted` + mpv baseline `prefetch-playlist=yes`); coordinator widens prefetch trigger to last-song-start (drops `<10s remaining` guard, `emitNowPlaying` calls `maybeStartPrefetch` so single-song promo/favorites/1-song-music blocks queue immediately); `absorbPrefetchResult` queues next block via `engine.queueNext` with post-await `currentBlock != nil` race guard (orphan playlist entry cleared if cleanup ran during suspension); `queuedToEngine` flag gates dual-path EOF handler (`.fileStarted` → `swapToPrefetchedBlockState` when queued; `.fileEnded(.eof)` → fallback `engine.play(replace)` only when not queued); `skipForward` past-last fast-paths to `engine.advanceToQueued` when queued; `changeChannel`/`stop`/`handlePlaybackError`/`advancePastUnplayableBlock`/`resume` long-idle all `clearPlaylist` + reset `queuedToEngine`; `swapToPrefetchedBlockIfAvailable` fallback's `engine.play` throw now wipes state + emits to `errorsContinuation` (was silent log): 384
- After PR 29 Update checker — `UpdateChecker` actor (startup probe + 24h `tickIfDue` ticker; `checkNow` issues `GET releases/latest` with GitHub API headers, filters draft/prerelease, parses tag via `SemVer`, persists `lastUpdateCheckAt` + `cachedLatestRelease`, emits via multi-subscriber `AsyncStream<UpdateState>`); `SemVer` (parse strips `v`/`V` prefix + `-prerelease` suffix) + `GitHubRelease`/`GitHubReleaseAsset` Decodable + `ReleaseInfo` (failable from-release init picks first `*.dmg` asset case-insensitive) + `UpdateState` (.unknown / .upToDate / .available); `AppSettings.updateCheckEnabled` (default true) + `lastUpdateCheckAt` + `dismissedUpdateVersion` + `cachedLatestRelease`; dismiss persistence + auto-reset on higher version; `NoopUpdateChecker` injected when unbundled (no `CFBundleShortVersionString`); `StubURLProtocol.registerError(url:error:)` added; `SettingsViewModel` Updates section (toggle, Check Now, last-checked relative + version status line); `MiniPlayerViewModel.updateButtonVisible`/`updateAvailableForMenu`/`openUpdatePanel`/`requestOpenUpdatePanel` (dismiss flips on every panel open path); `MiniPlayerView` channelRow shows bordered "Update Available + arrow.up.forward.square" button when visible (else "RP Player" text); `ContextMenuBuilder` adds sticky "Update Available…" item between About and Quit when menu surface set; `UpdatePanelController` (NSPanel + Esc dismissal + content swap) hosts `UpdatePanelView` (title, relative published date, ~5-line markdown body, [Later] / [View Full Notes] / [Download DMG]; DMG button hidden when asset missing); `AppDelegate` late-binds `openUpdatePanel`; `UpdateChecker` subscribes to `configStore.changes` so toggle OFF clears `cachedLatestRelease` + resets state to `.unknown`; `UpdatePanelController` dismisses on Esc + outside click; `MiniPlayerViewModel.requestOpenUpdatePanelFromMenu` (sync, no dismiss) splits the menu path from the popover-button path so menu clicks don't suppress the popover button; popover button drops the `arrow.up.forward.square` icon (was overlapping the channel picker) and gains hover affordance via `UpdateAvailableButton` sub-view (`@State hovering` + 0.12s ease-in-out animation; stroke opacity 0.5→0.85 + background fill 0→0.12 on hover); Settings panel: when update available, "Check Now" is replaced with "Open Update…" button that calls `checkNow()` then `openUpdatePanel()` so users don't need to leave Settings to act on the update; `SettingsViewModel.updateAvailable` flag + `openUpdatePanel` closure (late-bound by `AppDelegate`); version-line trims "— open Update Available menu" hint since the action is now self-contained: 432
- After PR 30 Long-idle resume stall guard — `LivePlaybackCoordinator` stall watchdog (`armLongIdleStallWatchdog` + `waitForFirstPositionUpdate` via `withTaskGroup` race + `surfaceStallError`) armed only in `resume()`'s long-idle/expired branch; injected `sleep` closure for deterministic tests; `cancelStallWatchdog` wired to 8 cleanup sites; 6 new tests via `ControllableSleep` helper (NSLock + `withCheckedContinuation` wrapped in `withTaskCancellationHandler` so cancelled Tasks always resume parked continuations) covering clear-on-positionUpdate / single-timeout retry / double-timeout error / stop+pause cancellation / no-arm-on-fresh-resume: 438
- PR 30 release sidecar (v0.5.3): mpv `network-timeout=15` baseline option + `networkTimeoutOptionForTesting()` helper + readback test were dropped because the macOS-14 GH runner aborted with `freed pointer was not the last allocation` SIGABRT inside `MpvPlayerEngineTests`. Eventually traced to the macos-15→macos-26 runner toolchain bump in `ef0c341`, not the watchdog scaffolding (test file revived in `7bf0529` after CI went green on macos-26). Re-introduce the option in a future PR if the watchdog alone proves insufficient.
- After PR 31 api/gapless migration — adds `GaplessSong` + `GaplessResponse` types + `RpApiClient.gapless`; `LivePlaybackCoordinator` rebuilt around `queue: [GaplessSong]` (drops 7 block-centric properties); `MPV_EVENT_START_FILE`-driven boundary advance with per-song `update_history` telemetry; `kickRefetch` on bootstrap / channel change / long-idle resume / `queue.count < 3`; race-guard checks both `currentChannelId` AND `queue.first?.eventId`; `handleSongPlaybackError` (replaces `advancePastUnplayableBlock`); `NowPlaying` simplified to `song: GaplessSong` + `songDurationSeconds` + `bitrateLabel`; `UpcomingProgramViewModel` switched to single `api.gapless` per channel; drops `RpApiClient.play` + `RpApiClient.getBlock` + `PlayAction` + `GetBlock` + `BlockSongs` + stale-block detection (PR 24); `PlayListSong` kept for notification-click info path with new `init(from: GaplessSong)` extension; `MockRpApiClient` extended with `gaplessByChannel` + delay knob; test factories hoisted to `Tests/RPPlayerTests/Helpers/GaplessFactories.swift`. PR 30 stall watchdog preserved verbatim. Net test delta: 438 → 371 (-67; large drop because block-shaped tests like `songIndexInBlock` / stale-block detection / prefetch+swap / multi-block stitching had no gapless analog): 371
- After PR 32 SongFileCache + download-then-play — adds `SongFileCache` protocol + `LiveSongFileCache` actor (cache tests covering download/HTTP-error/empty-body/evict/clear/cached-file/in-flight dedup/cancellation) + `MockSongFileCache` helper + coordinator integration tests (play-routes-through-cache, queue-advance-evicts-dropped-song, queue-advance-queues-next-via-cache, skip-sync-refetch-via-cache, change-channel-clears-cache, stop-clears-cache, shutdown-clears-cache, handle-playback-error-clears-cache, fallback-to-remote-on-cache-failure, sequential-downloader-walk, evict-after-telemetry). Code-review follow-ups: `LiveSongFileCache.downloadAndStore` checks `Task.isCancelled` before/after the request to avoid orphan files when the coordinator's `downloaderTask` is cancelled mid-flight; `handleEngineEvent` `.fileEnded(.eof)` + `handleSongPlaybackError` + `runRefetch` + `applyBitrateChange` all route remaining `engine.queueNext` sites through the cache (bitrate change additionally cancels the downloader + clears the cache since new bitrates mint new URLs); dead `_ = self` removed from `kickSequentialDownload`. Net delta: 371 → 396 (+25): 396
- After PR 32 follow-ups — `kickSequentialDownload` walk capped at 2 ahead (`queue.dropFirst().prefix(2)`) so a fresh 20-song gapless response no longer downloads the entire queue in background; `LiveSongFileCache` gains LRU cap `maxFiles=10` (default via `defaultMaxFiles`, configurable via `init(maxFiles:)`) — `evictOldestIfNeeded()` sweeps by `contentModificationDate` after every successful write. Test delta: existing `testPlayKicksSequentialDownloadOfRemainingQueue` renamed to `testPlayKicksSequentialDownloadOfAtMostTwoAhead` with 5-song queue + assertions that queue[3] / queue[4] are NOT downloaded; new `testEvictsOldestWhenOverMaxFiles` (5 songs into a maxFiles=3 cache → 3 most recent survive). 396 → 397: 397
- After PR 32 post-smoke-test fixes — (1) `SongFileCache.expectedLocalPath(for:)` (nonisolated) lets `syncQueueHeadFromMpv` match mpv's `path` property against both the remote `gaplessUrl` AND the deterministic local cache path; without this dual-match the queue head never advanced past queue[0] (UI stuck, telemetry repeated, mpv playlist exhausted on the 2nd skip). (2) `prefetchUpcomingSongArt` extended from queue[1] only to queue[1]+queue[2] so album-art prefetch matches the 2-ahead song-file prefetch depth. (3) `SongFileCache.cancelInFlightDownloads()` added — cancels every in-flight `Task<URL?, Never>` so `URLSession.data` actually stops streaming bytes (previously cancelling the coordinator's `downloaderTask` only cancelled the outer Task; the cache's inner Tasks kept running and ate bandwidth from the new channel's queue[0] download). Coordinator's stop / changeChannel / handlePlaybackError / applyBitrateChange / shutdown sites now call `cancelInFlightDownloads()` instead of `clear()` — files persist across channel changes so return-to-channel reuses cached songs; LRU is the only deletion mechanism. Test delta: `testBoundaryAdvanceMatchesByLocalFilePath` (uses real `LiveSongFileCache` in a tmp dir + new `MockPlayerEngine.setSimulatedCurrentPath(raw:)` to drive mpv reporting a bare file path); `testCancelInFlightDownloadsCancelsActiveTasks`; 4 lifecycle tests renamed from `…ClearsSongFileCache` → `…CancelsInFlightDownloads` (assert `cancelInFlightCalls` instead of `clearCalls`). 397 → 399: 399
- After PR 33 long-idle resume rework — `kickRefetch` filter generalized to `queue.last.eventId` (snapshot pair race-guard, merge as `self.queue + filtered`); `resume()` long-idle branch rewritten (cache-miss fallback to legacy refetch+restart with full state wipe; otherwise `engine.resume()` + `updateHistory(pauseFlag: true)` telemetry via `Task.detached` + reset `pausedAt/pausePositionMs` + truncate queue to `prefix(2)` + cancel downloader + fire-and-forget `cache.cancelInFlightDownloads()` + `kickRefetch()`); PR 30 stall watchdog wholly deleted (`armLongIdleStallWatchdog`, `cancelStallWatchdog`, `waitForFirstPositionUpdate`, `logStallWatchdogTimeout`, `surfaceStallError`, `stallWatchdog` field, `stallWatchdogTimeoutSeconds` constant, `sleep:` init param + `private let sleep` field, all 7 `cancelStallWatchdog()` call sites, `LivePlaybackCoordinatorStallWatchdogTests.swift` file with inline `ControllableSleep` + 6 tests, `markQueueHeadPending` dead helper, `currentSongInQueueAvailable` dead helper). New tests: `testKickRefetchFiltersByQueueLastEventIdAndAppendsTail`, `testLongIdleResumePreservesCachedSongAndQueueOne`, `testLongIdleResumeMergesFreshTailAfterRefetch`, `testLongIdleResumeWithCacheMissForQueueZeroFallsBack`, `testLongIdleResumeKicksRefetchAfterShortQueue`, `testSecondResumeDuringInFlightRefetchIsIdempotent`. Test seam: `internal snapshotQueueIds()` accessor (via `@testable import RPPlayer`); `MockSongFileCache.cachedFileOverride: (@Sendable (GaplessSong) -> URL?)?` eviction hook; `Tests/RPPlayerTests/Helpers/WaitUntil.swift` polling helper. Net: 400 → 399 (+6 added, −7 deleted: `skip_testStallWatchdogStillArmsAfterLongIdleResume` + 6 watchdog suite tests).
- After PR 34 Volume mode picker — adds `VolumeMode` enum + custom Codable migration paths on `AppSettings` and `AudioProfile` (legacy `forceMaxVolumeEnabled` + `applyReplayGainEnabled` decoded into the enum at load time, written back only as `volumeMode`); `SettingsViewModel` drops two bool published vars + setters for one `volumeMode: VolumeMode` + `setVolumeMode(_:)`; SettingsView replaces two toggles with a 3-Button row (`StableButtonStyle`, matching Appearance / Menu bar icon / Popover style rows) — Force Max button uses `.disabled(!hogModeEnabled)`, action handler fires destructive-confirm alert directly. Single ⓘ `HoverInfoIcon` to the left of the buttons with combined tooltip text. AppContainer binder reads enum directly (effective-RG collapses to `volumeMode == .replayGain`); hog OFF→ON ground-truth check is demote-only. New tests: 2 enum codable, 7 AudioProfile migration, 6 AppSettings migration, 4 VM volumeMode surface. 399 → 421 (+22).
- After PR 35 Parametric EQ MVP — `setAudioFilterChain` engine surface (1 test); EqModels Codable / equality / raw values (3); strict parser 9-case suite including unsupported-type / >10-band / malformed reject and OFF-skip / empty-file (9); writer + chain builder + Filter-numbering regression (8); EqPresetStore (9); AudioProfile EQ migration (2); SettingsViewModel EQ surface (7); AppContainerEqBinder integration (2). 421 → 462 (+41 added; 461 functional, 1 pre-existing network flake unchanged).
- After PR 36 Crossfeed for headphone listening — `CrossfeedFilterBuilder` defaults / non-default trimmed-fractions / out-of-range clamp (3); `EqChainBuilder.buildParts` array-return rewrite (6 existing tests rewritten, no count delta); `AudioProfile` crossfeed migration: defaults-when-absent / round-trip / PR-35 EQ-only profile upgrade / PR-34 legacy bool migration still works (4); `AppContainerAudioFilterBinder` integration: 2 existing EQ-only tests retained + 4 new tests for crossfeed-only chain, EQ+crossfeed combined chain (order Preamp → EQ → Crossfeed locked), both-off clears chain, strength-change rewrites chain (6 total); `SettingsViewModel` crossfeed surface: initial-props-reflect-active-profile + 3 setter write-through + clamp + no-op-without-uid (6); `ClampedNumericFieldLogic` parse dot/comma locale / parse rejects garbage / validity range + NaN / format %.2f (5). Final count 489; net PR-36 contribution +22 (3 + 4 + 4 + 6 + 5; the EqChainBuilder rewrite is in-place, no count delta). Pre-PR-36 baseline on `main` was higher than the 462 recorded for PR 35 (likely late-PR-35 fix-ups not back-annotated to the test-count log) — the +22 is the canonical PR-36 contribution.
- After hog-mode sample rate enforcement — `HogModeController` gains `internal private(set) var originalSampleRate: Double?`; `acquire` reads + stores pre-hog rate and sets 44100 Hz after hog confirmed; `releaseHog` restores original rate (no settle) and clears storage; `setSampleRate(rate:deviceUID:)` delegates to new private `setSampleRateInternal(_:deviceID:settle:)`. New tests: `testAcquireWithUnknownUIDLeavesOriginalSampleRateNil`, `testReleaseWithoutAcquireDoesNotMutateOriginalSampleRate`. 489 → 491.
- After PR 37 Device reattach when hog — `DeviceReattachTests` (5 tests: handleDeviceLost preserve/reset branches + spawnReattachWatcher reappear + cancellation) + `SettingsViewModelDisconnectedRowTests` (4 tests: nil/present/synth/fallback label). 491 → 500 (+9).
- After PR 38 BS2B crossfeed upgrade — vendored libmpv rebuilt with `libbs2b`; new `CrossfeedProfile` enum + 3 tests (`CrossfeedProfileTests`); `CrossfeedFilterBuilderTests` rewritten with bs2b emission cases (5 tests, was 4); `AudioProfileCrossfeedMigrationTests` adds the legacy `crossfeedStrength`/`crossfeedRange` → cmoy-defaults migration test + the encoded-JSON-omits-legacy-keys test (5 tests, was 4); `AppContainerAudioFilterBinderTests` rewritten for bs2b chain emission (7 tests, same count); `SettingsViewModelCrossfeedTests` rewritten for the new profile/fcut/feedDb setters (6 tests, same count). Branch base was pre-PR-37 (491); bs2b-only contribution: 491 → 498 (+7).
- After PR 38 UI polish — `CrossfeedProfile.bs2bDefault` case removed (UI Default button dropped; Custom now seeds 700 Hz / 4.5 dB on entry from any other profile); `AudioProfile` decode adds a legacy `crossfeedProfile=="default"` → `.custom` + seed migration path; `SettingsViewModel.setCrossfeedProfile` seeds fcut/feedDb on entry-into-custom transitions; SettingsView gains `bitrateRow` + `hogModeRow` helpers (replacing inline `Picker`/`Toggle`) with `HoverInfoIcon` tooltips; crossfeed Custom row right-aligned via `.frame(maxWidth: .infinity)` HStack + `Spacer(minLength: 0)` lead + opacity-0 Toggle anchor; `ClampedNumericField` widened to 72×22 + explicit 12pt font + new `decimals: Int = 2` parameter (fcut field passes `decimals: 0`). Test deltas: `CrossfeedProfileTests` (3 tests, was 3 — case-list shrunk); `CrossfeedFilterBuilderTests` (4 tests, was 5 — dropped bs2bDefault assertion); `AudioProfileCrossfeedMigrationTests` (6 tests, was 5 — new legacy-default migration test); `SettingsViewModelCrossfeedTests` (8 tests, was 6 — new entering-custom-seeds + reselect-custom-preserves tests); `ClampedNumericFieldTests` (6 tests, was 5 — new explicit-decimals format test). 498 → 502 (+4).
- After merging PR 38 into main (post-PR-37) — combined PR 37 device-reattach (+9) and PR 38 bs2b + UI polish (+11) deltas. 500 → 511.
- After PR 39 EQ preset editor (2026-05-21) — per-preset edit panel with grid bands (Type / Frequency / Gain / Q + trash per row, plus Add band); filter-type dropdown (Bypass / Peak / Low Shelf / High Shelf) with Bypass → enabled=false round-tripping as `OFF` rows in `.txt`; Save / Save As / Rename actions; live audio preview via `EqEditingOverride` actor side-channel; `EqPresetStore.rename(from:to:)` API + 30-char name cap (was 255 — UI label length is the binding constraint, longer import filenames truncate on first save); parser now preserves `OFF` rows as `enabled: false` (was silently skipped); writer emits `OFF` rows with sequential Filter-N indexing over all bands; `applyAudioFilterState` signature widened to read the override channel; old `AudioFilterKey` struct removed (binder now diffs directly against the override-aware key tuple); confirmation alert when overwriting a preset referenced by 2+ audio profiles (`presetReferenceCount` helper + `testPresetReferenceCountCountsDevicesUsingName`). 511 → 540 (+29).
- After PR 40 EOF recovery non-blocking (2026-05-22) — `.fileEnded(.eof)` recovery in `LivePlaybackCoordinator` no longer awaits `songFileCache.localFile(for: next)` on the actor (was blocking on in-flight downloads, causing a silent-playback cascade when a short promo's recovery queued behind the next song's download). Now uses synchronous `cachedFile(for:)`; cache miss → defer `queueNext` + emit `.loading`. New `tryQueueNextIfPending(landed:)` helper fires from `kickSequentialDownload` after each download lands, logs `elapsedSinceDeferMs`, lifts state from `.loading` → `.playing`. Cascade-protection lift at end of recovery branch handles queue collapse to <2 before the queueNext branch could re-defer. New diagnostic logs: recovery-entry cache state, downloader-landed marker. New tests: failing-test for eof-recovery non-blocking queueNext, cascade-protection regression, mock `markDownloaded` aligned with production `cachedFile` semantics. 540 → 543 (+3).
- 2026-05-22: 543 → 548 (+5) — PR 41 tryQueueNextOrDefer helper (4 per-site defer + 1 lift; skipForward shallow-refetch test dropped due to kickRefetch race)
- 2026-05-23: 548 → 559 (+11) — PR 42 cache-aware transport buttons + stop action. Coordinator-level `nextReady` signal: initial / play-with-cached / play-deferred / stop / stream-replay (5). `skipForward` no-`.loading` regression (1). MiniPlayerViewModel: `nextReady` mirroring + `stopPlayback()` clears visuals & preserves channel (2). MockPlaybackCoordinator `nextReady` initial + `fireNextReady` (2). Play-after-stop state machine regression (1).
- 2026-05-23: 559 → 572 (+13) — PR 43 EQ preset picker switch while editing. `SettingsViewModelEqEditTests`: cancelPendingSwitch clears pending without touching editor (1), requestPresetSwitch on clean editor reseeds to target (1), requestPresetSwitch with editor closed sets preset without dialog (1), requestPresetSwitch with same target is no-op (1), requestPresetSwitch clean editor → Bypass closes editor (1), requestPresetSwitch on dirty editor sets pendingPresetSwitch (1), requestPresetSwitch while pending active is ignored (1), resolvePendingSwitchDiscard swaps and clears pending (1), resolvePendingSwitchSave writes original then swaps (1), resolvePendingSwitchSave on new preset is no-op (1), resolvePendingSwitchSaveAs writes new name then swaps to user's target (1), resolvePendingSwitchSaveAs switches to Bypass if target was nil (1), requestPresetSwitch on new preset dirty sets pendingPresetSwitch (1).
- 2026-07-01: 572 → 592 (+20) — PR 44 skip low-rated songs. `SkipPolicyTests`: disabled policy never skips (1), unrated songs never skipped (1), below-threshold skips (1), at-or-above threshold kept (1). `SkipSettingsTests`: defaults (1), missing keys decode to defaults (1), round-trip (1). `SettingsViewModelSkipTests`: initial props reflect snapshot (1), setSkipLowRatedEnabled persists toggle (1), setSkipRatingThreshold persists value (1). `PlaybackCoordinatorSkipTests`: skip-bound songs excluded from queue (1), all-skipped stops and emits no-matches message (1), head becoming skip-bound mid-playback is auto-skipped (1), disabled policy plays low-rated songs normally (1), applyBitrateChange filters skip-bound songs (1). `MiniPlayerViewModelSkipTests`: rating below threshold triggers skip (1), rating at threshold does not skip (1), rating below threshold with feature disabled does not skip (1). `UpcomingProgramViewModelSkipTests`: rows marked skipped by policy (1), no rows skipped when disabled (1).
